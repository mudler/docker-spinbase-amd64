machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t sabayon/spinbase-amd64 .
    - docker export $(docker run -i -t sabayon/spinbase-amd64 true && docker ps -aq | xargs echo | cut -d ' ' -f 1) | docker import - sabayon/spinbase-amd64
test:
  override:
    - docker run sabayon/spinbase-amd64-squashed true
    - docker run sabayon/spinbase-amd64 true
deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push sabayon/spinbase-amd64
